#include<stdio.h>
// multiline paragraphs
void main()
{
    //char str[100] = "Apple";
    //puts(str);
    char str[50];
    printf("Enter Any String : ");
    //scanf("%s",str);
    gets(str);
    puts(str);
}
